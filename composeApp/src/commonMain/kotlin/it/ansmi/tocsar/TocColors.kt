package it.ansmi.tocsar

import androidx.compose.ui.graphics.Color

/** Colori allineati a TocAppBuild (Flutter). */
object TocColors {
    val Background = Color(0xFF050505)
    val Surface = Color(0xFFF4EFF6)
    val Red = Color(0xFFD91F2A)
    val GpsAccent = Color(0xFF0B3D7A)
    val TextDark = Color(0xFF121212)
    val TextBody = Color(0xFF2A2630)
}

/** Texture vegetato: stessa opacità della TOC Flutter. */
const val VegetatoTextureOpacity = 0.44f
